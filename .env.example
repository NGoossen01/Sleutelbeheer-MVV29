# Sleutelbeheer-MVV29 — omgevingsvariabelen voor de API server
# Kopieer dit bestand naar .env en pas de waarden aan.
# In Docker/TrueNAS worden deze ingesteld via docker-compose.yml.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=sleutelbeheer
DB_USER=sleutel
DB_PASSWORD=changeme_password
PORT=4000
NODE_ENV=production
